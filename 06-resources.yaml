apiVersion: v1
kind: Pod
metadata:
  name: resources-demo
spec:
 containers:
 - name: nginx
   image: nginx
   resources:
    # softlimit
   requests:
    memory: "64Mi" # RAM
    cpu: "250m"   # CPU
  
   # hardlink
   limits: 
    memory: "128Mi" # RAM
    cpu: "500m"    # CPU