spec:
  replicas: 3
  selector:
    matchLabels:
      project: roboshop
      purpose: rb-demo
      tier: frontend
      component: frontend

  template:
    metadata:
      labels:
        project: roboshop
        purpose: rb-demo
        tier: frontend
        component: frontend
    spec:
      containers:
      - name: nginx
        image: nginx
