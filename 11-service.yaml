apiVersion: v1
Kind: service
metadata:
  name: nginx
spec:
 selector:
    # labels are used as containers
       project: roboshop
       component: frontend
       environment: dev
ports:
 - ports:
 -  port: 80
    targetPort: 80
